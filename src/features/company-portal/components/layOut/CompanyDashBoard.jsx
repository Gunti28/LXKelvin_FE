import { Outlet } from "react-router-dom";
const CompanyDashBoard = () => {
  return (
    <div>
      CompanyDashBoard
      <main>
        <Outlet />
      </main>
    </div>
  );
};

export default CompanyDashBoard;
